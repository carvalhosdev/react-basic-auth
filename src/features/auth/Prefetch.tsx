import { Outlet } from "react-router-dom"

const Prefetch = () => {

    // useEffect(() => {
    //     //later
    //     //store.dispatch(authApi.util.prefetch('getUserDetailV2', 'users', {force: true}))
    //     },[])
    return <Outlet />
}

export default Prefetch